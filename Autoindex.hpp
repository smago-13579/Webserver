#ifndef AUTIONDEX_HPP
# define AUTIONDEX_HPP

class Autoindex
{
    public:
        Autoindex();
        Autoindex(Autoindex const &);
        Autoindex &operator=(Autoindex const &);
        ~Autoindex();

    private:

};

#endif